# Intent: maestro

## Purpose

Help an agent discover participating Copilot sessions in its current CMUX workspace, send fire-and-forget messages, and reply using the supplied return address. Explain launch permissions, unsupported/offline recipients, and that a send is not a delivery or completion guarantee. Never change focus, interfere with human input, or grant permissions implicitly. Reuse existing Maestro lifecycle guidance rather than duplicating it.
